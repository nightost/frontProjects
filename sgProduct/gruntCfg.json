{
    "imagemin":{
        "files":[{
            "expand": true,
            "cwd": "<%=config.srcDir%>/images/",
            "src": ["**/*.{png,jpg,jpeg}"],
            "dest": "<%=config.distDir%>/images/"
        }]
    },
    "sass":{
        "files":[{
            "expand": true,
            "cwd": "<%=config.srcDir%>/images/",
            "src": ["**/*.{png,jpg,jpeg}"],
            "dest": "<%=config.distDir%>/images/"
        }]
    }
}